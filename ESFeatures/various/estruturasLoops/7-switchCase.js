const letsSwitchCase = Key => {
  const key = Key;
  switch (key) {
    case 12:
      return (resultKey = key);
    case 24:
      return (resultKey = key);
    case 77:
      return (resultKey = key);
    default:
      break;
  }
};

console.log(letsSwitchCase(34));
